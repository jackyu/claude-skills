# 掌握度報告模板

Heptabase 卡片內容照這個結構寫，第一行必須是 h1。本機 fallback 檔用同一份，但去掉 `<hepta-table>` 標籤、toggle 的 `+` 改成 `-`。

~~~markdown
# [審查測驗] <MR 標題>

#<標籤1> #<標籤2>

MR：<web_url>（沒 MR 時寫：branch `<branch>`，commit `<first>..<last>`，MR 開了再補）
考試日：<YYYY-MM-DD>

## 這輪考了什麼

- 對照 `origin/main`，改動 <N> 個檔案
- 題數：<M> 題，✅ <a>／🟡 <b>／❌ <c>
- 主要難度落在：<一句話，例「樂觀更新與快取回滾」>

## 掌握度

<hepta-table tokenId="new" hasColumnHeader="true">
| 觀念 | 位置 | 狀態 | 一句話備註 |
| --- | --- | --- | --- |
| 樂觀更新三段式 | `use-cancel-order.ts:24-40` | 半懂 | 講得出回滾，沒講 onSettled 為什麼要再 invalidate |
| GET /orders 回應契約 | `orders.schema.ts:8` | 懂 | — |
| OrderCard props 改動的影響範圍 | `order-card.tsx:12` | 不懂 | 跳過；不知道還有誰在用 |
</hepta-table>

狀態只有三種：懂／半懂／不懂。跳過的題目歸「不懂」並在備註寫「跳過」。

## Reviewer 可能追問的三題

上 MR 前自己再答一次。挑本輪答得最不穩的觀念延伸，不重複已考過的原題。

1. <問題>
2. <問題>
3. <問題>

## 補課紀錄

只列走過導師模式的觀念。每條：講了什麼重點（2–3 句）＋換角度重問的結果。

- **樂觀更新三段式**：onMutate 先 cancelQueries 免得舊請求回來蓋掉快取，快照是為了 onError 回滾，onSettled 再 invalidate 是背景校正。重問「onError 的 context 誰給的」→ ✅。

## 值得保留的洞見

使用者答得比標準答案好、或提出更周延做法的地方。沒有就整段刪掉，不硬寫。

- <內容>

## 下一步

- 不懂的觀念要不要先補再開 MR：<建議>
- 可以接 `/fe-mr-digest` 的素材：<觀念列表>

---

## 完整考古題

從 `qa-log.md` 原封搬過來，日後複習重看用。每題一個 toggle，題目當標題、展開才是內容；不摘要、不改寫使用者的回答。

+ Q1 🟢 為什麼這樣做 — `use-orders.ts:24` 為什麼要 `cancelQueries` 再寫快取？
  - **回答**：<使用者原文>
  - **評分**：🟡 部分對
  - **講評**：<少了什麼、對在哪>

+ Q2 🟡 API 契約 — `GET /orders` 回的 `status` 有哪些值？
  - **回答**：<使用者原文>
  - **評分**：✅
  - **講評**：<一句>

+ 補課後重問 — `onError` 拿到的 context 是誰給的？
  - **回答**：<使用者原文>
  - **評分**：✅
~~~

## 取捨原則

- 掌握度表每列一個觀念，不是一題一列；同一觀念考了兩題（原題＋重問）合併成一列，狀態取重問後的結果。
- 表格每格一段行內文字，不能塞清單或換行；備註寫不下就縮成一句，細節留在考古題那題的講評。
- 「Reviewer 可能追問的三題」是報告最實用的一段，不能省。
- 分隔線以上的報告段控制在 60 行內；超過代表這輪考太廣，下次縮範圍。分隔線以下的考古題不受行數限制，有幾題寫幾題。
